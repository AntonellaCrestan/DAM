import { CambiarAmayusculaPipe } from './cambiar-amayuscula.pipe';

describe('CambiarAmayusculaPipe', () => {
  it('create an instance', () => {
    const pipe = new CambiarAmayusculaPipe();
    expect(pipe).toBeTruthy();
  });
});
